# 停止イベント記録テンプレート

仕様書13章「ログ・監視」の要件（全停止イベントに時刻・状態・停止理由・最後の指令・SDK結果を
残す）に対応。停止・異常が発生する都度、このテンプレートをコピーして記入する。
rosbagの記録と合わせて保管し、後から突き合わせられるようにする。

---

## イベント記録

- **日時**: __________
- **フェーズ**: Phase 0 / 1 / 2a / 2b / 2c / 3 / 4（該当を丸で囲む）
- **記録者**: __________

### 状態

- **停止直前の状態(仕様書7章)**: DISCONNECTED / STANDBY / READY / NAVIGATING / FAULT / E_STOP
- **停止理由**:
  - [ ] E-stop入力(物理/純正リモコン)
  - [ ] E-stop入力(ソフトウェア `/g1/estop`)
  - [ ] 指令タイムアウト(cmd_timeout超過)
  - [ ] SDK通信断・APIエラー連続発生
  - [ ] TFの欠落・期限切れ
  - [ ] センサーデータの期限切れ
  - [ ] Collision Monitorの停止指令
  - [ ] 転倒またはLocomotion Controllerの重大異常
  - [ ] 操作者による意図的な停止
  - [ ] その他: __________

### 直前の指令・SDK結果

- **最後に送られた速度指令**: vx=______ vy=______ omega=______
- **最後のSDK呼び出し結果**: 成功 / 失敗(エラー内容: __________)
- **`sdk_error_count`(連続失敗回数)**: __________

### 事象の詳細

何が起きたか(観測された事実のみ。憶測は書かない):

```



```

### 対応

とった対応:

```



```

### 復帰

- [ ] 原因を特定した(内容: __________)
- [ ] `/g1/clear_fault` または手動解除で復帰した
- [ ] 復帰後、低速から再開した
- [ ] このフェーズの試験を中断し、設計を見直した(Planning.mdへの反映: __________)

### rosbagとの対応

- **rosbagファイル名**: __________
- **該当タイムスタンプ**: __________

---

## 再発防止(該当する場合)

このイベントが設計上の見落としに起因する場合は、`nav2_option/FAILURES.md` にも
テンプレートに沿って記録すること(こちらは実機試験のイベント記録、FAILURES.mdは
開発中の失敗記録という役割分担)。
