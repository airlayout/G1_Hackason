# Phase 0 チェックシート(接続性検証)

[Planning.md Phase 0](../Planning.md) の作業項目に対応。実施日・実施者を記入し、
`incident_log_template.md` と合わせて保管する。

- 実施日: __________
- 実施者: __________
- 停止担当者: __________

## 開始前

- [ ] `roles.md` の役割割り当てを完了した
- [ ] `test_area.md` の基準で試験区域を確保・立入管理を開始した
- [ ] G1を安全支持具で固定した
- [ ] 純正リモコンの動作確認をした(roles.md参照)
- [ ] G1の機種・エディション・ファームウェア版を記録した: __________
- [ ] SDK2のバージョンを固定した: __________

## 作業項目(Planning.md Phase 0 に対応)

- [ ] **1.** 機種・ファームウェア・SDK2版の記録(上記と同じ)
  - SDK2(C++, `~/work/unitree_sdk2`)バージョン: `2.0.2-70-g9754cd1`(2026-09-09確認、コマンド: `git describe --tags --always`)
  - G1機種・エディション・ファームウェア版: **未確認**。PC2で`unitree-upgrade`というWeb UI(`http://192.168.123.164/`)が動いているのを発見したが、JSアプリでAPI叩きが必要なため、ファームウェア更新ツールに不用意に触れるリスクを避け、ブラウザで直接確認することを推奨(2026-09-09)
- [x] **2.** 公式サンプルで `Move()` と停止が動作することを確認した(ROS非介在)
  - 観察結果: **OK**(2026-09-09)。ROS を介さず `g1_loco_client` のみで動作・停止を確認した
  - 開始時の状態は `fsm_id=0`(ZeroTorque) → 立ち上げ後 `fsm_id=501` / `mode_machine=5`
  - ⚠️ **`GetBalanceMode()` は機能していない**。開始時から一貫して `65535`(=0xFFFF、未対応の番兵値)を返し、`--balance_stand` を送っても変わらない。`fsm_id`/`fsm_mode` は正しく返るので getter 全般の問題ではない。**バランスモードの確認手段が無い**ことを前提に進めること
- [x] **3. 【最重要・U-07】** `SetVelocity()` を1回だけ送り、以後は何も送らずに
  `duration` 満了後の挙動を観察した
  - 送信した速度: **vx=0.2 m/s (vy=0, omega=0)** / duration: **1.0** 秒
  - 満了後の挙動(自動停止 / 直前速度を保持 / その他): **自動停止した**（指令を追加送信していないのに自分で止まった）
  - 実際に停止するまでの時間: **活発な歩容 1.910 秒 / 完全静止まで 3.419 秒**
    （LowState の脚12関節を 1017Hz で記録して計測。歩容中の最大関節速度 6.10 rad/s、
    関節位置の最大変化 23.2°。静止時のベースラインは 0.038 rad/s）
  - → Planning.md §3.3 のどちらの対応表に該当するか: **「`duration` 満了後に自動的に停止する」＝安全側の分岐**。
    D-27 の方針のまま、SDK 側 watchdog を冗長層として運用してよい。
    有線 LAN 必須・純正リモコン常時待機を受入要件に格上げする必要は無い
  - ⚠️ **ただし止まるまでに時間がかかる**: `duration` 1.0 秒に対し**関節の動きは 1.9〜3.4 秒**続き、
    **満了後に約2秒の"尾"が残る**。D-16 の「歩容途中の停止で1〜2歩進む」という想定と整合する
  - ⚠️ **訂正(2026-09-09)**: ここに当初「0.2 m/s なら停止距離は 0.2〜0.4m 程度」と書いたが**誤り**。
    「関節が1.9秒動き続ける」ことから「その間ずっと指令速度で進む」と外挿したのが間違いだった。
    **実測した停止距離は 4.3〜7.0cm**（0.3 m/s 指令、下記の項目7の追試を参照）。
    **脚は動き続けるが胴体はほとんど進まない**（最後の数歩は足を揃えて止まるための動作）
  - 📌 **副産物(U-12 の部分解)**: **0.1 m/s では歩容が成立しなかった**（関節が 0.4° しか動かず、
    姿勢の微応答のみ。0.077〜0.2 秒で終わる）。**0.2 m/s では成立した**（23.2°）。
    つまり歩容の下限速度は **0.1〜0.2 m/s の間**にある
- [x] **4.** SDK側プロセスにROSライブラリが混入していないことを確認した
  (`ldd`/`nm`チェック。[g1_sdk_bridge_cpp/README.md](../g1_sdk_bridge_cpp/README.md)参照)
  - 結果: **OK**(2026-09-09、PC2実機環境)。`g1_sdk_bridge_cpp`を`~/nav2_bridge_cpp/`にデプロイしビルド(ROS環境非source、D-06/D-08準拠)。単体テスト39件全通過。`ldd`で`rmw`/`rclcpp`/`ament`/`ros`のいずれも検出されず(リンク先は`libpthread`/`libstdc++`/`libm`/`libgcc_s`/`libc`のみ)
- [x] **5.** Unix domain socketの往復レイテンシを実測した
  - 実測値(2026-09-09、PC2 Orin NX上、SOCK_SEQPACKET、n=2000往復、3回試行の代表値): **p50=145us / p95=147us / p99=150us / max=302us**
    (20Hz周期=50,000usに対して余裕99.7%)
- [ ] **6. 【重要・U-09】** MID-360の取付角度を実測し、死角範囲を数値で確定した
  - 取付角度(前傾/水平等): __________
  - 死角範囲(足元〜何m先まで床が見えないか): __________
  - (未実施。現物の角度計測が必要)
- [x] **7.** G1 stateのodometryを記録し、座標系・単位・符号を確認した(一次確認)
  - **使った情報源**: SDK2 の `SportModeState_` には位置・速度が無い(U-02)ため、
    **内蔵SLAM の `/unitree/slam_mapping/odom`**(`1801` で起動、9.1Hz)を使った
  - 前進指令で x が正に増えるか: **YES**。**手で正面へ押して +1.159 m**、そのとき
    Δy=+0.012m / Δyaw=+0.15° ＝ ほぼ純粋な並進として記録された(2026-09-09)
  - 左指令で y が正に増えるか: **直接の単独計測はしていない**。ただし右手系・z上向きで
    「x=前方」「yaw正=反時計回り」が確定しているので **y=左** が導かれる。
    手動での2回目の操作(Δx=-0.615 / Δy=+0.952 / Δyaw=+92.93°)も矛盾しない
  - 反時計回り指令で yaw が正に増えるか: **YES**。`SetVelocity(0,0,+0.3,1.0)` で **+10.20°**
  - 📌 **odom の品質**: 静止時は 106 秒間 ±0.005m / ±0.1° 以内で安定。
    1.16m の手押しを余計な yaw 回転 0.15° で追跡できた
  - ⚠️⚠️ **未解決・要追試**: **`vx=0.2, vy=0, omega=0` と指令したのに、実際の進行方向が
    機体前方から約 +98.6° ずれた**(Δx=-0.018 / Δy=+0.142 / 距離 0.143m)。
    **利用者の観察では「ずれていたが歩けていた」** ＝ 歩容そのものは成立しており
    (関節が 23.2° 動いている)、**進行方向が大きく外れた**ということ。
    手で押せば 1.16m 素直に前進する(Δyaw +0.15°)ので、支持具が並進を妨げているのではない。
    - **これは速度の未達より深刻**: Nav2 の controller は「指令方向 ≒ 実際の進行方向」を
      前提にしている。方向が 90° ずれるなら、Regulated Pure Pursuit(D-22)は経路を追えない
    - **ただし1回の試行のみ**。低速(歩容の成立下限すぐ上)固有の現象か、恒常的な偏りか、
      単発のつまずきかを区別できていない
    - **追試した(2026-09-09、0.3 m/s × 3回)。下記のとおり低速固有の現象だったと判明**

- 📌 **追試: 0.3 m/s での前進(U-08 / U-12 / 方向のずれ、2026-09-09)**
  指令 `SetVelocity(0.3, 0, 0, 1.0)` を10秒以上あけて3回。odom(9.1Hz)と脚関節(1017Hz)を同時記録。

  | 回 | Δx | Δy | 距離 | Δyaw | 進行方向のずれ | duration中の移動 | **停止距離** |
  |---|---|---|---|---|---|---|---|
  | 1 | +0.230 m | -0.093 m | 0.248 m | -1.25° | -20.4° | 0.209 m | **0.044 m** |
  | 2 | +0.231 m | -0.090 m | 0.248 m | -5.77° | -18.0° | 0.208 m | **0.043 m** |
  | 3 | +0.228 m | -0.122 m | 0.259 m | -1.90° | -17.7° | 0.240 m | **0.070 m** |

  - ✅ **0.2 m/s での「進行方向が 98.6° ずれる」現象は低速固有だった**。0.3 m/s では
    ずれが **-17.7〜-20.4°** に収まり、Δx は **3mm 以内**で再現する
  - ✅ **U-08(停止距離)の一次実測: 0.3 m/s 指令で 4.3〜7.0 cm**。停止まで 3.1〜4.5 秒
  - ✅ **速度追従率 70〜80%**(duration 1.0秒の間に 0.208〜0.240m ＝ 0.21〜0.24 m/s)。
    Planning.md の sim 実測(76%)と整合する
  - ⚠️ **系統的な右への偏りが残る**: 3回すべて右へ約19°veerし、yaw も毎回 -1.3〜-5.8°(時計回り)
    ずれる。ランダムなばらつきではない。**原因(機体の較正／支持具の引っ張り／床の傾き)は未切り分け**。
    Nav2 の経路追従では無視できないので、Phase 1 で切り分けること
  - ⚠️ **odom の瞬間速度が指令の175〜183%(0.52〜0.55 m/s)を示す**が、平均は 0.21〜0.24 m/s。
    **9Hz の odom が歩容の上下・前後動を拾ったノイズ**と考えられる。速度フィードバックに
    使うならローパスが必要(D-14 の「速度判定にローパス済み推定速度を使用」と整合)
  - 📌 **U-11 の一次データ**: `omega=+0.3` の旋回(+10.20°)に伴う並進ドリフトは **4.4 cm**
- [x] **8.** Orin NXのリソース状況を確認した(CPU/メモリ/他プロセス負荷)
  - 結果(2026-09-09): CPU 8コア、ほぼアイドル。メモリ15GiB中13GiB空き。ROS 2 Foxy/Noeticインストール済みだが現在プロセス無し。ディスク1.9TB中46GB使用

## 完了条件の確認

- [ ] SDK公式サンプルでG1が動き、停止できる
- [ ] 指令途絶時のG1挙動が実測値として記録され、設計分岐(§3.3)が確定している
- [ ] SDK側プロセスにROSライブラリが混入していない
- [ ] MID-360の死角範囲が数値で確定している

全て確認できたらPhase 1へ進む。1つでも未達成の場合は原因を特定し、Planning.mdの
該当D-xx/U-xxを見直す。

## 異常があれば

`incident_log_template.md` に記録すること。
