# 実行するたびに増える検知ログ。中身は運用の記録なので git には入れない
# （日付ごとに増え、複数人が動かすと必ず衝突する）
Log/*.jsonl

# サンプル動画は git に入れない（2026-09-20 の判断）。UI 開発には要るが、
# リポジトリを重くしないため各自が Reference/ に置く。
# ⚠️ 置かないと Main/run.sh が「動画が見つかりません」で止まる
Reference/*.mp4
