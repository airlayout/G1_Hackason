<!--
  ⚠️⚠️ **このファイルは使われていない（2026-09-09 に共通版へ移行）。**
  実機と共有する 1 本は `Navigation/nav2/navigate_g1.xml` で、sim の nav2.yaml も
  そちらを指している。このファイルは **Jazzy 専用**（WouldAPlannerRecoveryHelp /
  WouldAControllerRecoveryHelp / error_code_id は Humble に無い）で実機に載らない。
  **消していないのは、共通版の Jazzy での読み込みをまだ確認できていないため**
  （.200 が 2026-09-09 時点でネットワークに居ない）。確認が取れたら消す。
  それまでは nav2.yaml の 1 行を戻せばこちらに復帰できる。
  ⚠️ **こちらを編集しないこと。** 直すのは共通版のほう。

  G1 用の Behavior Tree。

  Nav2 標準の navigate_to_pose_w_replanning_and_recovery.xml をもとに、
  復帰動作から BackUp（後退）を取り除いてある。

  G1 の歩行ポリシーは後退が学習範囲外で、-0.3 m/s 以上の後退で転倒する
  （実測。詳細は G1/CLAUDE.md）。標準の BackUp は 0.15 m/s なので
  転倒はしないが、behavior_server 側で backup プラグインを外してあるため
  ノード型が解決できず BT の読み込みに失敗する。

  詰まったときは Spin（その場旋回）と Wait で復帰させる。
  旋回は指令と実測がほぼ一致する（±1.0 -> ±0.98）ので信頼できる。
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <ControllerSelector selected_controller="{selected_controller}" default_controller="FollowPath" topic_name="controller_selector"/>
        <!-- 既定プランナは Smac2D（2026-09-09 に採用）。同条件の Isaac Sim 実測で
             NavFn（GridBased）0/3 に対し Smac2D 3/3。1 区間目は出発地点もゴールも
             同一で、NavFn が 351 s で残り 2.95 m の時間切れだった区間を 79 s で
             残り 0.04 m まで詰めた。勝ち方は「速い」ではなく「詰まっても抜ける」
             （計画失敗 18 回すべてをリカバリ後の再探索で解いた）。
             詳細: docs/作業ログ/2026-09-09_1_プランナ比較_Smac2Dで到達.html
             ⚠️ NavFn に戻したいときは yaml を触らず planner_id で選ぶ。
             check_navigation.py の planner-id オプション、または
             /planner_selector トピックへ GridBased を発行する
             （具体的なコマンドは README と作業ログにある。
             ⚠️ XML のコメントにはハイフン 2 つを書けないため、
             ここにコマンド行そのものは置けない）。 -->
        <PlannerSelector selected_planner="{selected_planner}" default_planner="Smac2D" topic_name="planner_selector"/>
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="{selected_planner}" error_code_id="{compute_path_error_code}"/>
            <Sequence>
              <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
              <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{path}" controller_id="{selected_controller}" error_code_id="{follow_path_error_code}"/>
          <Sequence>
            <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
            <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
          </Sequence>
        </RecoveryNode>
      </PipelineSequence>
      <Sequence>
        <Fallback>
          <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
          <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
        </Fallback>
        <ReactiveFallback name="RecoveryFallback">
          <GoalUpdated/>
          <RoundRobin name="RecoveryActions">
            <Sequence name="ClearingActions">
              <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap"/>
              <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
            <Spin spin_dist="1.57" error_code_id="{spin_error_code}"/>
            <Wait wait_duration="5.0"/>
          </RoundRobin>
        </ReactiveFallback>
      </Sequence>
    </RecoveryNode>
  </BehaviorTree>
</root>
