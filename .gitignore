# Python
__pycache__/
*.py[cod]

# 実行ログ（実行のたびに増えるため管理対象外）
logs/
*.log

# 学習済み checkpoint（SimEnvTest/checkpoints/ は共有のため追跡対象。それ以外の .pt は除外）
*.pt
!SimEnvTest/checkpoints/*.pt

# SLAM の作業中に保存した確認用の地図（完成版のみ手動で追加する）
maps/check*
*_preview.png
